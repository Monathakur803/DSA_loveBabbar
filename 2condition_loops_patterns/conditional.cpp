#include <iostream>
using namespace std;
int main()
{
   /*
   //declearing an integer type variable
   int score;

   //print Enter the score
   cout<< "Enter the score "<<endl;

   //taking input from user
   cin>> score;

   //if condition
   if(score < 300) {
      cout<<"India wins "<<endl;
   }

   cout<<"Pakistan wins" <<endl;
*/
   /*
      int age ;
      cout<<"Enter your age ";
      cin>>age;
      if (age>=18) {
         cout<<"You are eligible for vote";
      }
      else {
         cout<<"You are not eligible for vote";
            }
   */
   /*
   // using nester if and else

   int marks;
   cout << "Enter your marks: ";
   cin >> marks;
   if (marks >= 90)
   {
      cout << "A grade";
   }
   else
   {
      if (marks >= 80)
      {
         cout << "B grade";
      }
      else
      {
         if (marks >= 60)
         {
            cout << "C grade";
         }
         else
         {
            if (marks >= 40)
            {
               cout << "D grade";
            }
            else {
               cout<<"Fail";
            }
         }
      }
   }
*/
   /*
   // using else if statement
   int marks;
   cout<<"enter your marks ";
   cin>>marks;
   if (marks>=90){
      cout<<"A grade";
   }
   else if (marks>=80){
      cout<<"B grade";
   }
    else if (marks>=60){
      cout<<"C grade";
   }
   else if (marks>=40){
      cout<<"D grade";
   }

   else{
      cout<<"Fail";
   }
   */

   return 0;
}